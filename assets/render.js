(()=>{
const root=document.querySelector('#app')||document.body;
const meta=window.GC_META, units=window.GC_UNITS;
const statusLabel={verified:'Verified booking',contact:'Rent by contact','no-current':'No verified current rental'};
const money=n=>n==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const unitHref=u=>`units/${encodeURIComponent(u.unit)}.html`;
function approxSeries(r){
  const start=r.tableStartYear||2026, tax=Object.fromEntries(Object.entries(r.tax||{}).map(([k,v])=>[+k,+v]));
  const years=[]; for(let y=2026;y>=start;y--) years.push(y);
  const keys=Object.keys(tax).map(Number).sort((a,b)=>a-b); const latest=keys.length?Math.max(...keys):null;
  function taxBasis(y){
    if(tax[y]) return tax[y];
    const below=keys.filter(k=>k<y).pop(), above=keys.find(k=>k>y);
    if(below&&above){const t=(y-below)/(above-below);return Math.round(tax[below]+(tax[above]-tax[below])*t)}
    if(below) return tax[below]; if(above) return tax[above]; return null;
  }
  if(keys.length && r.currentApprox){
    const scale=r.currentApprox/tax[latest];
    return years.map(y=>({year:y,approx:y===2026?r.currentApprox:Math.round((taxBasis(Math.min(y,latest))*scale)/1000)*1000,published:tax[y]||null,pub:y===2026?'2026 preliminary — verify NCAD':(tax[y]?'Exact-unit portal':'Not exposed')}));
  }
  let historical=null;
  if(r.asking && r.asking.year<=start && r.asking.price) historical={year:r.asking.year,value:r.asking.price};
  if(r.lastSale && r.lastSale.price && r.lastSale.year<=start) historical={year:r.lastSale.year,value:r.lastSale.price};
  if(!historical) historical={year:start,value:Math.round(((r.currentApprox||0)*0.55)/1000)*1000};
  return years.map(y=>{
    const span=2026-historical.year||1, t=Math.max(0,Math.min(1,(y-historical.year)/span));
    const v=Math.round((historical.value+((r.currentApprox||historical.value)-historical.value)*t)/1000)*1000;
    return {year:y,approx:v,published:tax[y]||null,pub:y===2026?'2026 preliminary — verify NCAD':(tax[y]?'Exact-unit portal':'Not exposed')};
  });
}
function shell(content){return `<header class="top"><div class="wrap"><a class="brand" href="./">Grand Caribbean · Port A</a><small>Unofficial condo guide · researched ${esc(meta.researchedOn)}</small></div></header>${content}<footer class="footer"><div class="wrap">Unofficial research directory. Rental availability, STR status, prices, assessments and values change. Verify before booking or relying on property data.</div></footer>`}
function gallery(){return `<div class="gallery" aria-label="Shared views from Grand Caribbean"><img src="assets/images/hero.webp" alt="View from Grand Caribbean toward Mustang Island" loading="lazy"><img src="assets/images/marsh-view.webp" alt="Marsh and neighborhood view" loading="lazy"><img src="assets/images/neighborhood-view.webp" alt="Grand Caribbean exterior and nearby homes" loading="lazy"></div>`}
function card(u){
  const title=u.name?`${u.unit} · ${esc(u.name)}`:`Condo ${u.unit}`;
  const occ=u.occupancy||u.placard?.maxOccupancy;
  return `<article class="card" data-unit="${u.unit}" data-status="${u.status}" data-floor="${u.floor}" data-occ="${occ||0}"><span class="pill ${u.status}">${statusLabel[u.status]}</span><h2>${title}</h2><div class="facts"><span class="fact">${u.bedrooms} BR</span><span class="fact">${u.bathrooms} BA</span>${occ?`<span class="fact">Sleeps / max ${occ}</span>`:''}${u.str?`<span class="fact">STR ${esc(u.str)}</span>`:''}</div><p>${u.status==='verified'?`${u.bookings.length} validated rental channel${u.bookings.length===1?'':'s'}`:u.status==='contact'?'Onsite rental contact; no exact public book-now page verified':'Property/sale/value research included'}</p><a class="open" href="${unitHref(u)}">Open condo details</a></article>`;
}
function landing(){
 document.title='Grand Caribbean Condo Guide | Port Aransas';
 const counts=Object.fromEntries(['verified','contact','no-current'].map(s=>[s,units.filter(u=>u.status===s).length]));
 root.innerHTML=shell(`<section class="hero"><div class="wrap hero-grid"><div class="hero-copy"><div class="eyebrow">Mustang Island · Port Aransas, Texas</div><h1>Grand Caribbean condo directory</h1><p>Find an exact unit, see current rental channels, compare sleeping capacity and rooms, or inspect sale/value history when no public booking page can be verified.</p><div class="stats"><div class="stat"><strong>${counts.verified}</strong><br>verified online rentals</div><div class="stat"><strong>${counts.contact}</strong><br>rent-by-contact</div><div class="stat"><strong>${counts['no-current']}</strong><br>no current link found</div></div></div><img src="assets/images/hero.webp" alt="Grand Caribbean and Mustang Island view"></div></section><main class="section"><div class="wrap"><div class="notice"><strong>Link standard:</strong> a booking page is assigned to a condo only when the unit number, STR number, exact manager page or another exact-unit identifier matches. Onsite placard limits are kept separate from booking-site sleeping capacity when the two differ.</div><div class="filters"><input id="q" type="search" placeholder="Search 3008, Beach Haven, STR…" aria-label="Search condos"><select id="status"><option value="">All rental states</option><option value="verified">Verified booking</option><option value="contact">Rent by contact</option><option value="no-current">No verified rental</option></select><select id="floor"><option value="">All floors</option>${[1,2,3,4].map(n=>`<option value="${n}">Floor ${n}</option>`).join('')}</select><select id="occ"><option value="">Any occupancy</option><option value="4">Sleeps 4+</option><option value="6">Sleeps 6+</option><option value="8">Sleeps 8+</option></select></div><div id="resultCount" class="source-note"></div><div id="cards" class="grid">${units.map(card).join('')}</div>${gallery()}<div class="method"><strong>Research method.</strong> ${esc(meta.methodology)}</div></div></main>`);
 const q=document.querySelector('#q'),status=document.querySelector('#status'),floor=document.querySelector('#floor'),occ=document.querySelector('#occ'),cards=document.querySelector('#cards'),rc=document.querySelector('#resultCount');
 function filt(){const term=q.value.trim().toLowerCase(), st=status.value, fl=floor.value, oc=+occ.value||0; const list=units.filter(u=>{const occupancy=u.occupancy||u.placard?.maxOccupancy||0; const blob=[u.unit,u.name,u.str,u.placard?.manager,u.placard?.phone,...(u.bookings||[]).map(b=>`${b.channel} ${b.details}`)].filter(Boolean).join(' ').toLowerCase();return (!term||blob.includes(term))&&(!st||u.status===st)&&(!fl||String(u.floor)===fl)&&(!oc||occupancy>=oc)}); cards.innerHTML=list.map(card).join('')||'<div class="empty">No matching condos.</div>';rc.textContent=`Showing ${list.length} of ${units.length} condos`;}
 [q,status,floor,occ].forEach(el=>el.addEventListener('input',filt));filt();
}
function bookingBlock(u){
 if(u.status==='contact')return `<div class="notice"><strong>No exact public book-now page verified.</strong> An onsite rental contact was photographed; use the placard reference below and confirm current availability directly.</div>`;
 if(!u.bookings.length)return `<div class="notice warn"><strong>No exact live booking page found.</strong> This search did not verify a current booking link for this unit; it does not mean short-term rental is legally prohibited.</div>`;
 return `<div class="link-list">${u.bookings.map(b=>`<div class="booking"><strong>${esc(b.channel)}${b.secondary?' · secondary channel':''}</strong><p>${esc(b.details)}</p><a href="${esc(b.url)}" target="_blank" rel="noopener">Open ${esc(b.channel)}</a><div class="validation">${esc(b.validation)} · checked ${esc(b.checked)}</div></div>`).join('')}</div>`;
}
function placardBlock(u){if(!u.placard)return '';const p=u.placard;return `<h2>Onsite placard reference</h2><div class="booking placard"><div class="badge-row">${p.maxOccupancy?`<span class="fact">Posted max ${p.maxOccupancy}</span>`:''}${p.str?`<span class="fact">STR / registration ${esc(p.str)}</span>`:''}</div><p>${p.manager?`Manager/contact: <strong>${esc(p.manager)}</strong>. `:''}${p.phone?`Phone: <a href="tel:${p.phone.replace(/[^0-9]/g,'')}">${esc(p.phone)}</a>. `:''}${p.note?esc(p.note):''}</p><div class="validation">Transcribed from onsite photo, ${esc(p.checked||meta.researchedOn)}.</div></div>`}
function realtyBlock(u){const r=u.realty;if(!r)return '';const rows=approxSeries(r);return `<h2>Sale, value & tax record</h2><div class="notice warn"><strong>Why this section appears:</strong> no exact current public booking page was verified during the research sweep. Property pages can still say rentals are allowed or show historical rental use.</div><div class="value-cards"><div class="value-card"><small>Current approx. value</small><strong>${money(r.currentApprox)}</strong><small>${esc(r.currentBasis)}</small></div><div class="value-card"><small>Latest/known sale</small><strong>${r.lastSale?String(r.lastSale.year):'Not surfaced'}</strong><small>${r.lastSale?esc(r.lastSale.label):'No exact sale record found in indexed sources'}</small></div><div class="value-card"><small>History table starts</small><strong>${r.tableStartYear}</strong><small>${esc(r.startLabel)}</small></div></div>${r.asking?`<p class="sale-line"><strong>${r.asking.year} listing:</strong> ${money(r.asking.price)} — ${esc(r.asking.label)}</p>`:''}<div class="source-links">${r.links.map(l=>`<a href="${esc(l.url)}" target="_blank" rel="noopener" title="${esc(l.note)}">${esc(l.label)}</a>`).join('')}<a href="${esc(r.officialTaxUrl)}" target="_blank" rel="noopener">Official Nueces CAD</a></div>${r.notes?.map(n=>`<p class="source-note">${esc(n)}</p>`).join('')||''}<h2>Annual value table</h2><div class="table-wrap"><table class="history"><thead><tr><th>Year</th><th>Approx. market value</th><th>Published assessed / taxable value*</th><th>Tax-data status</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${x.year}</td><td class="model">${money(x.approx)}</td><td>${x.published?money(x.published):'—'}</td><td class="muted">${esc(x.pub)}</td></tr>`).join('')}</tbody></table></div><p class="source-note"><strong>*Tax-value note:</strong> public realty portals commonly expose an assessed value, not every jurisdiction-specific exemption-adjusted taxable value. Exact values surfaced for this unit are shown; missing years remain blank. Use the linked Nueces CAD record to verify certified taxable values. <strong>Approx-value method:</strong> ${r.modelBasis==='tax-scaled'?'the current unit-value anchor is normalized to the exact-unit published assessment series; missing assessment years are interpolated only for the market-value model, never presented as published tax values.':'a straight-line historical model is used where a sufficiently complete exact-unit assessment series did not surface.'} 2026 county values are preliminary until certification.</p>`}
function detail(u){
 document.title=`Unit ${u.unit}${u.name?` · ${u.name}`:''} | Grand Caribbean Condo Guide`;
 const d=document.querySelector('meta[name="description"]'); if(d)d.content=`Grand Caribbean Unit ${u.unit} in Port Aransas: ${u.bedrooms} bedrooms, ${u.bathrooms} baths, rental references, occupancy and property history.`;
 const ix=units.findIndex(x=>x.unit===u.unit), prev=units[ix-1], next=units[ix+1];
 const title=u.name?`Condo ${u.unit} · ${esc(u.name)}`:`Condo ${u.unit}`;
 const bookingOcc=u.occupancy?`${u.occupancy} guests`:'Not advertised';
 root.innerHTML=shell(`<main class="detail"><div class="wrap"><a class="back" href="./#units">← All condos</a><div class="detail-head"><div><div class="eyebrow">Floor ${u.floor}</div><h1>${title}</h1><div class="badge-row"><span class="pill ${u.status}">${statusLabel[u.status]}</span>${u.str?`<span class="fact">STR / registration ${esc(u.str)}</span>`:''}</div></div><div class="summary-card"><dl><div><dt>Bedrooms</dt><dd>${u.bedrooms}</dd></div><div><dt>Bathrooms</dt><dd>${u.bathrooms}</dd></div><div><dt>Booking occupancy</dt><dd>${bookingOcc}</dd></div><div><dt>Interior</dt><dd>${u.sqft?`${Number(u.sqft).toLocaleString()} sf`:'Not verified'}</dd></div></dl></div></div>${u.beds?.length?`<div class="panel"><strong>Sleeping details</strong><p>${u.beds.map(esc).join(' · ')}</p></div>`:''}${u.note?`<p class="notice">${esc(u.note)}</p>`:''}<h2>${u.status==='no-current'?'Current rental search':'How to rent'}</h2>${bookingBlock(u)}${placardBlock(u)}${realtyBlock(u)}${gallery()}<nav class="unit-nav" aria-label="Adjacent condos"><span>${prev?`<a href="${unitHref(prev)}">← Unit ${prev.unit}</a>`:''}</span><a href="./#units">All condos</a><span>${next?`<a href="${unitHref(next)}">Unit ${next.unit} →</a>`:''}</span></nav><div class="method"><strong>Important:</strong> ${esc(meta.methodology)}</div></div></main>`);
}
const params=new URLSearchParams(location.search), id=document.body.dataset.unit||params.get('unit'); const u=id&&units.find(x=>x.unit===id); if(u)detail(u);else landing();
})();
